package demo

// DemoConfig is checked-in configuration for the at-rest scanner demo.
var DemoConfig = struct {
	APIKey string
}{
	// DEMO ONLY — intentional issue for Cursor Automations demo — remove after demo
	APIKey: "sk_demo_not_real_123",
}
